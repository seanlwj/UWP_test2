<Application
    x:Class="UWP31Test.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP31Test"
    RequestedTheme="Light">
    <Application.Resources>
        <Style TargetType="Image" x:Key="LogoImageStyle1">
            <Setter Property="Width" Value="200" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Source" Value="Assets/white-logo.png" />
            <Setter Property="Margin" Value="20,20,0,20" />
        </Style>
        <Style TargetType="Image" x:Key="LogoImageStyle2">
            <Setter Property="Width" Value="200" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Source" Value="Assets/color-logo.png" />
            <Setter Property="Margin" Value="20,20,0,20" />
        </Style>
        <Style TargetType="TextBlock" x:Key="TextBlockStyle1">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Application.Resources>

</Application>
